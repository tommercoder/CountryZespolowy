<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >
    <!-- This is the stroke you want to define -->
    <stroke android:width="2dp"
        android:color="@color/white"/>

    <!-- Optional, round your corners -->
    <corners android:bottomLeftRadius="9dp"
        android:topLeftRadius="9dp"
        android:bottomRightRadius="9dp"
        android:topRightRadius="9dp" />

    <!-- Optional, fill the rest of your background with a color or gradient, use transparent if you only want the border to be displayed-->
    <gradient android:startColor="@color/startgrad"
        android:endColor="@color/stopgrad"
        android:angle="180"/>
</shape>